import React, { Fragment } from 'react';

// COMPONENTS
import Header from '../components/Header/Header';

const mapping = () => (
    <Fragment>
        <Header />
        <div>Mapping</div>
    </Fragment>
);

export default mapping;